# Gptnotion

GptNotion modular TypeScript + Python/MCP project.

## Current Git baseline

`v3.0.0-modular`

Development policy:
- TypeScript strict
- no compile-error bypass
- Python MCP modules
- SQLite / RAG local backend
- `check.bat` before `build.bat`
- final frontend builds to a single `GptNotion.html`

The first preserved source baseline is on branch `baseline-v3.0.0-modular`.
